name: PyPI publishing disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    name: PyPI publishing disabled for 0.1.1.1
    runs-on: ubuntu-latest
    steps:
      - name: Explain distribution policy
        run: echo "AIKernel.Providers 0.1.1.1 is NuGet-only. Do not build or publish a PyPI package for this update line."
